name: oscar_web_resume
description: A web for my personal resume

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #Firebase
  google_fonts: ^1.1.2
  #Ui
  animated_text_kit: ^3.1.2
  responsive_builder: ^0.3.0
  dotted_border: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true

  assets:
    - lib/_core/assets/image/
