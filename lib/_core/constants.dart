import 'package:flutter/material.dart';

const pageDecoration = BoxDecoration(
  color: const Color(0xFFFFFFFF),
  boxShadow: [
    BoxShadow(
      color: Colors.grey,
      offset: Offset(0, 3),
      blurRadius: 2.5,
    )
  ],
);
